/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['*', 'localhost', 'lh3.googleusercontent.com', 'thumbs.dreamstime.com', 'cdn.shopify.com', 'res.cloudinary.com', 'giphy.com'],
      },
}

module.exports = nextConfig
